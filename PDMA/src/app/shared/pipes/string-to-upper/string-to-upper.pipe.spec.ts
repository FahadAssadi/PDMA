import { StringToUpperPipe } from './string-to-upper.pipe';

describe('StringToUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new StringToUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
